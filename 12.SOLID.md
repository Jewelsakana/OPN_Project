## SOLID 原则  
SOLID是五个面向对象设计原则组成的首字母缩略词，旨在使软件设计更易于理解，更具灵活性且更易于维护  
### 单一职责原则  
* 定义：一个类应该只有一个引起它变化的原因  
* 核心理念：每个类仅负责实现软件功能的单一部分，且该职责完全应该由该类封装  
* 优势：降低耦合度。提高代码的复用性和可测试性  
* 找到并分离这些职责是软件设计真正意义上的许多内容  
### 开闭原则  
* 定义：软件实体（类，模块，函数等）应该对扩展开放，对修改封闭  
* 核心理念：在不触动现有的源代码的情况下，通过添加新代码来改变系统的行为  
* 实现方式：通常依赖于接口或者抽象类  
当需求改变的时候，应该扩展行为，但不能改变旧的代码  
### 里氏替换原则  
* 定义：子类型必须能够替换掉它们的基类型  
* 核心概念：如果S是T的子类型，那么程序中的所有T对象都可以替换为S对象，而不会改变程序的正确性  
* （如果违反LSP）通常，显式if语句或if/else链用于确定对象的类型，以便可以选择适合该类型的行为。 
* 使用基类指针或引用的函数必须能够在不知道的情况下使用派生类的对象  
* 鼓励重用基类型，并允许在派生类中进行修改，而不会损坏其他组件  
* 子类型的真正定义是可替换的，其中可替换性由显式或者隐式契约定义  
* 要求：子类不能违反父类定义的行为契约  
### 接口隔离原则  
* 定义：不应该强迫客户端依赖它们不使用的方法  
* 核心理念：比起使用单一的胖接口，使用多个专门的接口（微接口）更好  
* 目的：减少不必要的依赖，避免波及效应  
### 依赖翻转原则  
* 定义：高层模块不应该依赖于低层模块，两者都应该依赖于抽象  
* 抽象不应该依赖于细节，细节应该依赖于抽象  
* 核心理念：面向接口编程而非面向实现编程，解开模块间的强耦合   
* 通过使用DIP，可以使代码对更改具有弹性，因此代码更容易维护  
  

