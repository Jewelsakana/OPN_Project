### 最后的任务  
实现用户交互类（即主程序），整理文件和生成Make文件  
**任务**：  
1. 实现用户交互层，让用户输入命令，让workSpace处理命令并实现命令，实现交互的循环  
2. 等待用户输入时显示>>  
3. 如果用户直接按回车（空输入），则直接进入下一次循环  
4. 将底层文件中的报错统一给OutputService类处理  
5. 修改ExitCommand给出退出的标记，从而实现退出循环结束程序  
6. 整理该文件夹的的所有文件，按照功能和任务进行处理  
7. 将整理后的文件编写Makefile文件，实现总体的编译  
8. 更新SUMMARY.md文件  
