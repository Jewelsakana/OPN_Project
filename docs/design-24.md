### 当前任务  
重构  
**任务**：
1. 装饰器模式被硬编码在了 EditorListCommand类中，现在它同时承担了“如何格式化时长”和“如何拼接字符串”的职责。以及根据上下文，命令类不应该实现具体的逻辑，应该将其拆开放入新的类中，应该创建一个专门负责输出装饰的类，让 EditorListCommand 只负责调用。而且拼接字符串的逻辑应该也外包出去，不要在内部实现。  
2. 