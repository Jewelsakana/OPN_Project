# 重构总结

## design-15 重构

按照 `design-15.md` 的设计要求，对文本编辑器项目进行了全面重构，涵盖 16 个源文件。重构遵循 SOLID 设计原则，消除了识别的主要代码坏味道。所有 9 个测试套件均通过。

### 新建文件（11 个）

| 文件 | 职责 |
|------|------|
| `include/StringUtils.h` + `src/StringUtils.cpp` | 字符串工具函数（trim, splitString, startsWith, toLower），消除 CommandParser 与 FileSystemService 之间的重复代码 |
| `include/CommandParserStrategy.h` + `src/CommandParserStrategy.cpp` | 策略模式：18 个命令各自独立的解析策略类，替代原来的 if-else 链 |
| `include/CommandFactory.h` + `src/CommandFactory.cpp` | 工厂模式：集中创建 Command 对象，消除 CommandController 中 ~130 行的 switch 语句 |
| `include/ConfigSerializer.h` + `src/ConfigSerializer.cpp` | 配置序列化/反序列化，从 FileSystemService 中分离 |
| `include/DirectoryService.h` + `src/DirectoryService.cpp` | 目录树遍历逻辑，从 FileSystemService 中分离 |
| `include/ConfigManager.h` + `src/ConfigManager.cpp` | 配置管理编排，从 WorkSpace 中分离 |
| `include/ObserverManager.h` + `src/ObserverManager.cpp` | 观察者管理基类，WorkSpace 继承自此基类 |

### 应用的设计模式

1. **策略模式** — `ICommandParserStrategy` 接口 + 18 个具体策略类，替代 CommandParser 中的 if-else 链
2. **工厂模式** — `CommandFactory` 集中管理命令对象创建，消除 Controller 中的并行 switch
3. **模板方法模式** — `TextCommand::recordDeletedText()` 统一预删除记录逻辑
4. **观察者模式** — `ObserverManager` 基类封装观察者管理，WorkSpace 继承复用
5. **备忘录模式** — `WorkspaceMemento` 配合 ConfigManager/ConfigSerializer 实现工作区状态持久化

### 消除的代码坏味道

- ✅ 重复代码（StringUtils 提取、recordDeletedText 模板方法）
- ✅ 过长方法（CommandParser::parse 从 ~140 行缩减到 ~20 行）
- ✅ 过大的类（FileSystemService 拆分 3 个类、WorkSpace 拆分 4 个类）
- ✅ 并行 switch 语句（策略模式 + 工厂模式消除）
- ✅ 数据泥团（Position 值对象封装 row/col）

---

## design-16 重构

按照 `design-16.md` 的要求，对仍需重构的 6 个问题进行深度重构。所有 9 个测试套件均通过，编译无警告。

### 1. CommandFactory 注册表模式（消除 switch）

**修改文件：**
- `include/CommandFactory.h` — 新增 `EditorCommandCreator` / `WorkSpaceCommandCreator` 函数类型，新增 `registerEditorCreator()` / `registerWorkSpaceCreator()` 静态注册方法
- `src/CommandFactory.cpp` — 用 `std::unordered_map` 注册表替代两个 switch 语句（原第 46 行 5-case 编辑器 switch，原第 81 行 13-case 工作区 switch）；`createEditorCommand()` 和 `createWorkSpaceCommand()` 改为查表调用
- `src/TextCommands.cpp` — 5 个编辑器命令自注册（Append, Insert, Delete, Replace, Show）
- `src/WorkSpaceCommand.cpp` — 10 个工作区命令自注册（Load, Save, Init, Close, Edit, EditorList, DirTree, Undo, Redo, Exit）
- `src/LogCommand.cpp` — 3 个日志命令自注册（Logon, Logoff, Logshow）

**效果：** 新增命令只需在对应的 .cpp 文件中添加注册 lambda，无需修改 CommandFactory。

### 2. Editor 接口增强（消除 dynamic_cast）

**修改文件：**
- `include/Editor.h` — 新增 `undo()`, `redo()`, `canUndo()`, `canRedo()` 虚方法，提供默认空实现
- `include/TextEditor.h` — 对应方法标记 `override`
- `src/WorkSpaceCommand.cpp` — `UndoCommand::execute()` 和 `RedoCommand::execute()` 不再需要 `dynamic_pointer_cast<TextEditor>`，直接通过 Editor 接口调用
- `src/WorkSpaceCommand.cpp` — `UndoCommand::undo()` 和 `RedoCommand::undo()` 的 TODO 已实现（调用 editor->redo() / editor->undo()）

**效果：** Undo/Redo 命令不再依赖具体 TextEditor 类型，遵循里氏替换原则（LSP）。

### 3. 空指针检查集中化

**修改文件：**
- `include/WorkSpaceCommand.h` — 新增 `protected` 方法 `checkWorkSpace()` 和 `getActiveEditorOrThrow()`
- `src/WorkSpaceCommand.cpp` — 添加两个方法的实现；10 个命令的 `execute()` / `undo()` 中 ~20 处重复的空指针判断替换为 `checkWorkSpace()` 调用
- `src/LogCommand.cpp` — 5 处空指针判断替换为 `checkWorkSpace()` 调用

**效果：** 消除了 ~30 行重复的 null check 模板代码，统一错误消息。

### 4. WorkSpace 职责拆分（四大协调器 + 门面）

**新建文件：**
| 文件 | 职责 |
|------|------|
| `include/FileCoordinator.h` + `src/FileCoordinator.cpp` | 文件加载/保存/初始化/关闭的业务逻辑 |
| `include/EditorCoordinator.h` + `src/EditorCoordinator.cpp` | 编辑器切换和查询（委托给 DocumentManager） |
| `include/LogCoordinator.h` + `src/LogCoordinator.cpp` | 日志启停和查询 |
| `include/ConfigCoordinator.h` + `src/ConfigCoordinator.cpp` | 配置保存/恢复 |

**修改文件：**
- `include/WorkSpace.h` — 引入 4 个协调器成员，39 个公开方法退化为委托调用
- `src/WorkSpace.cpp` — 构造函数按依赖顺序初始化服务层和协调器层；129 个公开方法实现简化为门面委托

**架构：**
```
WorkSpace (Facade)
  ├── EditorCoordinator → DocumentManager
  ├── FileCoordinator    → FileSystemService, DocumentManager, LoggerManager
  ├── LogCoordinator     → LoggerManager, OutputService
  └── ConfigCoordinator  → ConfigManager
```
跨协调器的操作（createMemento, restoreFromMemento, notifySessionStart）保留在 WorkSpace 中。

### 5. 删除 FileIOService 别名

**修改文件：**
- `include/WorkSpace.h` — 删除 `using FileIOService = FileSystemService;` 及其注释（为从未发生的拆分预留的投机代码）

### 6. 消除 ShowCommand const_cast

**修改文件：**
- `include/TextCommands.h` — ShowCommand 改为直接继承 `Command`（而非 `TextCommand`），存储 `const std::vector<std::string>&` 和 `TextEngine*`
- `src/TextCommands.cpp` — 移除 `const_cast`，构造函数直接绑定 const 引用

**效果：** ShowCommand 不再继承 TextCommand 的非 const `lines` 引用，消除了类型不安全的 `const_cast`。

### 测试结果

全部 9 个测试套件通过，所有 .o 文件和可执行文件已清除：
- test_commandparser ✅
- test_commands ✅
- test_documentmanager ✅
- test_engine ✅
- test_log ✅
- test_log_recovery ✅
- test_loggermanager ✅
- test_outputservice ✅
- test_workspace ✅

---

## design-17 重构

按照 `design-17.md` 的要求，针对 5 个代码坏味道进行了重构。全部 9 个测试套件通过，编译无警告。

### 1. 统一自注册 Lambda（消除重复）

**修改文件：**
- `include/CommandFactory.h` — 新增 8 个自注册宏：
  - 工作区命令：`REGISTER_WS_CMD_NOARGS`、`REGISTER_WS_CMD_FILENAME`、`REGISTER_WS_CMD_REQ_FILENAME`、`REGISTER_WS_CMD_PATH`、`REGISTER_WS_CMD_TARGET`、`REGISTER_WS_CMD_INIT`
  - 编辑器命令：`REGISTER_EDITOR_CMD_GUARDED`（带守卫条件的通用宏）、`REGISTER_EDITOR_CMD_SHOW`（ShowCommand 专用）
- `src/WorkSpaceCommand.cpp` — 10 段匿名 namespace 注册代码（~87 行）缩减为 10 行宏调用
- `src/LogCommand.cpp` — 3 段匿名 namespace 注册代码（~24 行）缩减为 3 行宏调用
- `src/TextCommands.cpp` — 5 段匿名 namespace 注册代码（~51 行）缩减为 5 行宏调用

**效果：** 全部 18 个命令的自注册代码从 ~162 行缩减到 ~18 行，消除了所有匿名 namespace 中的静态初始化变量。

### 2. 消除双重 setWorkSpace()

**修改文件：**
- `src/CommandController.cpp` — `executeCommand()` 中移除重复的 `wsCommand->setWorkSpace(workspace_)` 调用，仅保留 `CommandFactory::createFromParsed()` 中的设置

**效果：** 消除了工厂和控制器各调用一次 setWorkSpace 的冗余。

### 3. 拆分过长方法

**修改文件：**
- `include/ConfigSerializer.h` + `src/ConfigSerializer.cpp` — `loadConfig()` 提取 `parseConfigLine()` 私有方法，将键值对解析逻辑独立
- `include/WorkSpace.h` + `src/WorkSpace.cpp` — `restoreFromMemento()` 拆分为 `restoreOpenFiles()`、`restoreModifiedStates()`、`restoreLogState()` 三个私有方法
- `include/WorkSpaceCommand.h` + `src/WorkSpaceCommand.cpp` — `ExitCommand::execute()` 提取 `ensureNoUnsavedFiles()` 和 `trySaveConfig()` 两个私有方法

**效果：** 每个方法职责单一，`restoreFromMemento()` 从 ~47 行缩减为 3 行委托调用，`ExitCommand::execute()` 从 ~38 行缩减为 5 行。

### 4. 修正 WorkSpace 接口的抽象层次

**修改文件：**
- `include/Event.h` + `src/Event.cpp` — 新增 `static currentTimestampString()` 方法，封装时间格式化逻辑
- `src/WorkSpace.cpp` — `notifySessionStart()` 从 9 行手动时间格式化退化为 2 行调用；移除不再需要的 `<chrono>`、`<ctime>`、`<sstream>`、`<iomanip>` 头文件
- `include/LogCoordinator.h` + `src/LogCoordinator.cpp` — `showLog()` 增加 `activeFileName` 参数，将"空文件名则使用当前活动文件"的逻辑从 WorkSpace 下放到 LogCoordinator
- `src/WorkSpace.cpp` — `showLog()` 从 8 行退化为 1 行委托调用

**效果：** WorkSpace 门面不再包含底层格式化逻辑和业务判断逻辑，保持接口在一致的抽象层次。

### 5. 数据成员封装

**修改文件：**
- `include/TextCommands.h` — `TextCommand::lines` 和 `TextCommand::textEngine` 从 `protected` 改为 `private`，新增 `protected` getter `lines()` 和 `textEngine()`
- `src/TextCommands.cpp` — 子类中所有对 `lines`/`textEngine` 的直接访问改为通过 getter 调用

**效果：** 消除了 protected 数据成员，强化封装性；子类通过 getter 访问，便于未来修改内部实现。

### 测试结果

全部 9 个测试套件通过，所有 .o 文件和可执行文件已清除：
- test_commandparser ✅
- test_commands ✅
- test_documentmanager ✅
- test_engine ✅
- test_log ✅
- test_log_recovery ✅
- test_loggermanager ✅
- test_outputservice ✅
- test_workspace ✅

---

## design-18 重构

按照 `design-18.md` 的要求，针对 6 个代码坏味道进行了重构。全部 9 个测试套件通过，编译无警告。

### 1. 抽取公共文件系统头文件（消除代码重复）

**新建文件：**
- `include/FilesystemCompat.h` — 统一的条件编译块，根据编译器版本选择 `std::filesystem` 或 `std::experimental::filesystem`，并定义 `HAS_FILESYSTEM` 宏

**修改文件：**
- `src/DirectoryService.cpp` — 原有的 10 行条件编译块替换为 `#include "FilesystemCompat.h"`
- `src/WorkSpaceCommand.cpp` — 原有的 11 行条件编译块（含 GCC 8 注释）替换为 `#include "FilesystemCompat.h"`

**效果：** 消除了 ~21 行重复的条件编译代码，改为共享一个公共头文件。

### 2. 抽取重复排序 Lambda（消除代码重复）

**修改文件：**
- `src/DirectoryService.cpp` — 新增匿名 namespace 中的 `compareDirectoryEntries()` 自由函数，替代 `buildDirectoryTree()` 和 `buildDirectoryTreeStructure()` 中完全相同的 8 行排序 lambda

**效果：** 消除了 ~16 行重复的排序逻辑，两个方法均改为 `std::sort(entries.begin(), entries.end(), compareDirectoryEntries)`。

### 3. 消除无所事事的类（ConfigCoordinator）

**删除文件：**
- `include/ConfigCoordinator.h` — 仅包含 2 个透传方法声明
- `src/ConfigCoordinator.cpp` — 仅 14 行，完全是对 ConfigManager 的透传调用

**修改文件：**
- `include/WorkSpace.h` — 移除 `#include "ConfigCoordinator.h"` 和 `ConfigCoordinator configCoordinator_` 成员
- `src/WorkSpace.cpp` — 移除构造函数中 `configCoordinator_` 的初始化；`saveConfig()` 和 `loadConfig()` 改为直接调用 `configManager_`

**效果：** 调用链从 `WorkSpace → ConfigCoordinator → ConfigManager → ConfigSerializer` 简化为 `WorkSpace → ConfigManager → ConfigSerializer`，减少一层无意义的间接。

### 4. 删除遗留声明和未使用代码

**修改文件：**
- `include/WorkSpaceCommand.h` — 移除 `DirTreeCommand::printDirectoryTree()` 遗留声明（实现已移到 DirectoryService）
- `include/Model.h` — 移除无人调用的 `getName()` 和 `reset()` 虚方法
- `include/TextEngine.h` — 移除 `getName()` 和 `reset()` 的 override
- `include/DocumentManager.h` — 移除 `getName()` override、`getOpenFileCount()`、`getModifiedFileCount()`
- `include/DirectoryService.h` — 移除 `getName()` override
- `include/ConfigManager.h` — 移除 `getName()` override
- `include/ConfigSerializer.h` — 移除 `getName()` override
- `include/LoggerManager.h` — 移除 `getName()` override
- `include/FileSystemService.h` — 移除 `getName()` override
- `include/OutputService.h` — 移除 `getName()` override
- `src/DocumentManager.cpp` — 移除 `getOpenFileCount()` 和 `getModifiedFileCount()` 实现
- `tests/test_documentmanager.cpp` — `getOpenFileCount()` 替换为 `getOpenFiles().size()`；`getModifiedFileCount()` 替换为手动遍历 `getAllModifiedStates()` 计数；修复 `setFileModified` 对未打开文件的预存测试 bug
- `tests/test_outputservice.cpp` — 移除 `getName()` 断言
- `tests/test_loggermanager.cpp` — 移除 `getName()` 断言

**效果：** 清除了全部未被生产代码调用的遗留方法和声明，消除了投机性代码。

### 5. 清理无用注释

**修改文件：**
- `src/OutputService.cpp` — 移除空构造函数中的 `// 构造函数` 注释
- `src/DocumentManager.cpp` — 同上
- `src/LoggerManager.cpp` — 移除 `// 构造函数可以初始化状态` 注释
- `src/WorkSpaceCommand.cpp` — 移除 `LoadCommand::undo()` 和 `InitCommand::undo()` 中解释代码行为的冗余注释（"如果文件之前未打开…"）
- `src/CommandParserStrategy.cpp` — 移除 `// ===== 辅助函数 =====`、`// ===== 编辑器命令策略 =====`、`// ===== 工作区命令策略 =====` 三个分隔注释

### 6. 命令统一使用 WorkSpace 输出接口（消除类间亲密）

**修改文件：**
- `include/WorkSpace.h` — 新增 4 个输出门面方法：`outputError()`、`outputLine()`、`outputList()`、`outputTree()`
- `src/WorkSpace.cpp` — 实现这 4 个方法，均委托给 `outputService_`
- `src/CommandController.cpp` — `getOutputService().outputError()` → `outputError()`
- `src/ConfigManager.cpp` — `getOutputService().outputError()` → `outputError()`
- `src/LogCommand.cpp` — `getOutputService().outputLine()` → `outputLine()`（4 处）
- `src/LoggerManager.cpp` — `getOutputService().outputLine()` → `outputLine()`（4 处）
- `src/WorkSpaceCommand.cpp` — `getOutputService().outputList()` → `outputList()`；`getOutputService().outputTree()` → `outputTree()`；`getOutputService().outputError()` → `outputError()`（2 处）；`getOutputService().outputLine()` → `outputLine()`

**效果：** 所有命令类和服务类不再直接持有/访问 `OutputService` 引用，改为通过 WorkSpace 门面接口输出。`getOutputService()` 仅保留在一处内部连线（CommandFactory 为 ShowCommand 构造 `EditorCommandContext`）。真正实现了 Facade 封装，减少了类间耦合。

### 测试结果

全部 9 个测试套件通过，所有 .o 文件和可执行文件已清除：
- test_commandparser ✅
- test_commands ✅
- test_documentmanager ✅
- test_engine ✅
- test_log ✅
- test_log_recovery ✅
- test_loggermanager ✅
- test_outputservice ✅
- test_workspace ✅
